import { gameScores } from "../../components/ui/AppLayout";
import { Result } from "../../components/ui/ResultScreen";

type CalculateScoresProps = {
  roundScore: gameScores;
};

const CalculateScores = ({roundScore}: CalculateScoresProps) => {
if (Result.DRAW) {

} else if (Result.WIN) {
setScores(scores.userScore + 1)
} else {
  
}
return (
  //color dot
)
}

export default CalculateScores